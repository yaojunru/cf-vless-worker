interface:
  display_name: "CF VLESS Worker Deploy"
  short_description: "Deploy, verify VLESS, and create client bundles."
  default_prompt: "Use $cf-vless-worker-deploy to deploy this Worker, verify Google through VLESS, and generate a private client bundle."
